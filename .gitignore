# Build artifacts
build/
install/
log/
debian/
.deb_build/
*.deb
compile_commands.json

# IDE
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc

# Environment setup script (local only)
env.sh

# Claude-mem generated files (keep root CLAUDE.md)
**/CLAUDE.md
!/CLAUDE.md
.claude/

# Local config files (environment-specific)
*.local.*

# Hardware-in-the-loop test scripts (require live tactile board on USB).
# Local-only — pulled out of versioned set per the project's HIL policy.
hil_*.py
local_hil/
